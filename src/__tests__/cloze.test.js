import { describe, test } from 'vitest'
// All cloze tests live in cloze.test.jsx (JSX extension required for render).
describe('cloze (see cloze.test.jsx)', () => {
  test.todo('all tests moved to cloze.test.jsx')
})
